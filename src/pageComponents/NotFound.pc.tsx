import { Stack } from '@mui/material';
import { FC } from 'react';


const NotFoundPC: FC = () => {
    return (
        <Stack>
            Cannot find page!
        </Stack>
    )
}

export default NotFoundPC;